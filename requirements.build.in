# These are requirements to build the site.
black
invoke==1.2.0
libsass==0.16.0   # for building the theme
Markdown==3.0.1   # required for Pelican to support Markdown content
pelican==4.2.0
pelican-readtime==0.2.0
pip-tools==5.3.1


# When needed someday, include runtime dependencies, too.
# -r requirements.txt
