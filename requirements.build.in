# These are requirements to build the site.
black
invoke
libsass   # for building the theme
Markdown   # required for Pelican to support Markdown content
pelican
pelican-readtime
pelican-search
pelican-sitemap
pip-tools


# When needed someday, include runtime dependencies, too.
# -r requirements.txt
